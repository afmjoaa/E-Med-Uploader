<customWindow:BasePage x:Class="custom_window.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:custom_window.Pages"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:customWindow="clr-namespace:custom_window"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

     

    <Grid Margin="8" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Style="{StaticResource MaterialDesignTitleTextBlock}">ListBox</TextBlock>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <ListBox Grid.Column="0" IsEnabled="{Binding IsChecked, ElementName=EnableListBox}">
                <customWindow:ActionListControl Height="40" VerticalAlignment="Top" />
                <customWindow:ActionListControl Height="40" VerticalAlignment="Top" />
                <customWindow:ActionListControl Height="40" VerticalAlignment="Top" />
                <customWindow:ActionListControl Height="40" VerticalAlignment="Top" />
                <TextBlock>Old</TextBlock>
                <TextBlock>ListBox</TextBlock>
                <TextBlock>Full of junk</TextBlock>
            </ListBox>
            <CheckBox Name="EnableListBox" Grid.Column="0"  Grid.Row="1" IsChecked="True">Enabled</CheckBox>

             </Grid>






        <!--<TextBlock Style="{StaticResource MaterialDesignTitleTextBlock}" Grid.Row="2" Margin="0 32 0 0">ListView</TextBlock>
            <ListView Grid.Row="3">
                <ListViewItem>
                    Hello
                </ListViewItem>
                <ListViewItem>
                    World
                </ListViewItem>
                <ListViewItem>
                    :)
                </ListViewItem>
            </ListView>
        <TextBlock Style="{StaticResource MaterialDesignTitleTextBlock}" Grid.Row="4" Margin="0 32 0 0">ListView.GridView</TextBlock>
        
            <ListView ItemsSource="{Binding Items1}"  Grid.Row="5">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Code}" Header="Code" />
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                        <GridViewColumn DisplayMemberBinding="{Binding Description}" Header="Description" />
                    </GridView>
                </ListView.View>
            </ListView>-->
    </Grid>
</customWindow:BasePage>